pre go
    func main() {
        fmt.Println("hello & welcome <world>")
    }
end

pre python
    def greet(name):
        # quotes & angle brackets <stay> as text
        print(f"Hello, {name}!")
end

pre markdown
    # Heading

    A paragraph after a blank line — both the `#` line and the
    blank line above survive verbatim.

    ## Subheading
end
