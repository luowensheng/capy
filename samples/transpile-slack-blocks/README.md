# transpile-slack-blocks

Slack message DSL → Block Kit JSON payload. POST to a webhook or paste
into the Slack [Block Kit Builder](https://app.slack.com/block-kit-builder).

```sh
../../capy run lib.yaml script.capy
```
