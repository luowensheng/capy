# transpile-changelog

Per-version entries → Keep-a-Changelog markdown. Mode-A blocks for each
version; `added`/`changed`/`fixed`/`removed` are flat statements inside.

```sh
../../capy run lib.yaml script.capy
```
