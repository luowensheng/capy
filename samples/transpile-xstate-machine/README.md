# transpile-xstate-machine

State DSL → XState v5 machine definition in TypeScript. Plug into any
React/Vue/vanilla app via `@xstate/react` or `xstate`.

```sh
../../capy run lib.yaml script.capy > machine.ts
```
