# transpile-protobuf

Schema DSL → `.proto` file. Demonstrates a field pattern that begins with
a typed capture (the field type) — no leading function-name literal.

```sh
../../capy run lib.yaml script.capy
```
