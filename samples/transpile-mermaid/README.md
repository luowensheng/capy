# transpile-mermaid

Flowchart DSL → Mermaid syntax. Two edge shapes (`a -> b` and `a -> b :
"label"`) demonstrate priority — the longer pattern (with the label)
wins via "more literals consumed."

```sh
../../capy run lib.yaml script.capy
```
