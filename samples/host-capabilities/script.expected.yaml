# Generated by Capy — do not edit. Source: deploy.capy
apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkout-api
  labels:
    environment: production
    region: us-west-2
    version: v2.3.1
spec:
  replicas: 6
  template:
    spec:
      containers:
        - name: checkout-api
          image: registry/checkout-api:v2.3.1
          env:
            - name: DATABASE_URL
              value: postgres://db.internal/prod
            - name: REGION
              value: us-west-2
          # API keys baked in from the sibling secrets file.
          # In real life this would be a Secret reference, not a
          # literal — kept inline here so the demo is self-contained.
          args:
            - "--key=sk_live_alpha_e91f2c0a"
            - "--key=sk_live_beta_77d4ab19"
            - "--key=sk_live_telemetry_2bc88f01"
