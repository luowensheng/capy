# transpile-chrome-extension

Extension DSL → Chrome MV3 `manifest.json`. Combine with a small
`content.js` for a loadable extension folder.

```sh
../../capy run lib.yaml script.capy > manifest.json
# load the directory in chrome://extensions (developer mode)
```
