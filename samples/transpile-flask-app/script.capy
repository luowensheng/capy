# A mock Flask API in 4 declarative lines. Every handler is an
# ABSTRACT verb — never inline Python — so the same source survives
# if you swap the library to Express, FastAPI, Gin, etc.

port 8000

endpoint get  "/health"      health_check
endpoint get  "/users"       list_mock_users
endpoint post "/users"       create_user
