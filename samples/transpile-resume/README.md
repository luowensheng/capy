# transpile-resume

Resume DSL → markdown CV. Header info accumulates in context; experience
entries are Mode-A blocks; bullets use `-` as the leading literal.

```sh
../../capy run lib.yaml script.capy
```
